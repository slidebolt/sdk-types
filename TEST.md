# Package Level Requirements

Tests for the sdk-types project should verify:

- **Serialization**: Core types (Device, Entity, Command, Event) must serialize/deserialize correctly to/from JSON.
- **Data Model Consistency**: Field validation for IDs, labels, and timestamps.
